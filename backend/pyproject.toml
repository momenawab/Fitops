[tool.ruff]
target-version = "py314"
line-length = 100
exclude = ["migrations", ".venv", "node_modules", "staticfiles", "mediafiles"]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]

[tool.ruff.lint.isort]
known-first-party = ["config", "apps", "common"]
